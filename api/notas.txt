Criar Ambiente Virtual (pasta anterior)
python -m venv nomeprojeto


pip install fastapi uvicorn
 

#Arancar servidor (dentro da pasta)
uvicorn main:app --host 192.168.1.125 --port 8000 --reload


se for necessario
Ativar Ambiente (pasta anterior)
nomeprojeto\Scripts\activate
(se dentro da pasta)
Scripts\activate